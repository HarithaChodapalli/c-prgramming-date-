#include<stdio.h>
main()
{
	int i,n,max,min;
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	min=a[0];
	max=a[0];
	for(i=1;i<n;i++)
	{
	if(max>a[i])
	max=a[i];
	if(min<a[i])
	min=a[i];
    }
	printf("%d\n",max);
	printf("%d",min);
}
